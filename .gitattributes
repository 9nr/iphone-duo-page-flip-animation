# core.autocrlf=true treats every file as text unless told otherwise, and it
# silently stripped 158KB of CRs out of a PNG on the first commit. Never again.
* text=auto

*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.mp4 binary
