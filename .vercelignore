# The deploy is the page, its scripts and its artwork. Nothing else ships.

# offline tooling - render.py, regression.py
*.py
__pycache__/

# test and render output
frames/
*.mp4

# notes and local config
HANDOFF.md
.claude/

# the artwork stays off the deploy. It opens on the placeholders instead, and
# vercel.json points src/assets.js at an empty list so nothing 404s.
assets/
src/assets.js
